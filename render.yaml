services:
  - type: web
    name: projectarot
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn backend:app"
    envVars:
      - key: GEMINI_API_KEY
        sync: false